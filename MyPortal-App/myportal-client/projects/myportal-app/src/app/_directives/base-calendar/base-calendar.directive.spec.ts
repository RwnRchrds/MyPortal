import { BaseCalendarDirective } from './base-calendar.directive';

describe('BaseCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
