import { PortalViewDirective } from './portal-view.directive';

describe('PortalViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalViewDirective();
    expect(directive).toBeTruthy();
  });
});
